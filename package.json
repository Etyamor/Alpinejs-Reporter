{
  "name": "reporter",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "tailwind": "npx tailwindcss -i ./input.css -o ./style.css --watch",
    "dev": "vite",
    "build": "npx tailwindcss -i ./input.css -o ./style.css && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "tailwindcss": "^3.4.10",
    "vite": "^5.4.1"
  },
  "dependencies": {
    "alpinejs": "^3.14.1"
  }
}
